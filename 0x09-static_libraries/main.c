#include "main.h"

/**
 * main - This is the main entry point, used mainly to test my static library
 * Return: (0) Success otherwise failure
 */

int main(void)
{
	_puts("\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick");
	return (0);
}
